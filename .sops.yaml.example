keys:
  - &admin_yubikey age1replace-with-your-yubikey-recipient
  - &server_host age1replace-with-your-server-host-recipient

creation_rules:
  - path_regex: secrets/server/[^/]+\.(yaml|json|env|ini)$
    key_groups:
      - age:
          - *admin_yubikey
          - *server_host
